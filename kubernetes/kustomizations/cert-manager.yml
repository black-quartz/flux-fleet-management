apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager
  namespace: flux-system
spec:
  serviceAccountName: flux-platform-reconciler
  targetNamespace: cert-manager
  sourceRef:
    kind: GitRepository
    name: black-quartz-platform
  path: kubernetes/components/cert-manager
  interval: 10m
  prune: true
  dependsOn:
    - name: platform-identity
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cert-manager-config
  namespace: flux-system
spec:
  serviceAccountName: flux-platform-reconciler
  targetNamespace: cert-manager
  sourceRef:
    kind: GitRepository
    name: black-quartz-platform
  path: kubernetes/components/cert-manager/config
  interval: 10m
  prune: true
  dependsOn:
    - name: cert-manager
