apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: vault-deployment
  namespace: flux-system
spec:
  interval: 1m
  provider: github
  url: https://github.com/black-quartz/vault-deployment.git
  ref:
    branch: main
  secretRef:
    name: flux-system
