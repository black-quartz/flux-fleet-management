apiVersion: v1
kind: LimitRange
metadata:
  name: compute-limits
spec:
  limits:
    - type: Container
      default:
        cpu: "500m"
        memory: "2Gi"
      defaultRequest:
        cpu: "200m"
        memory: "512Mi"
      max:
        cpu: "2"
        memory: "8Gi"
      min:
        cpu: "10m"
        memory: "32Mi"
