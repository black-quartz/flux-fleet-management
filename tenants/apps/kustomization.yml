apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - app-resourceset.yml
configMapGenerator:
  - name: app-tenants
    namespace: flux-system
    files:
      - inputs.yml
generatorOptions:
  disableNameSuffixHash: true
