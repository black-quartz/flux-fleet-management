apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: rbac
  namespace: flux-system
spec:
  path: ./clusters/prod-us-1/rbac
  interval: 5m
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-fleet-management
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: flux-runtime-info
