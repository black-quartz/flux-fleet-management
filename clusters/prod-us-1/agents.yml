apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: agents
  namespace: flux-system
spec:
  dependsOn:
    - name: apps
  path: ./clusters/prod-us-1/tenants
  interval: 5m
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-fleet-management
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: flux-runtime-info
